{#
  Parameters:
    helper_id: (string)
    invalid: (boolean)
    required: (boolean),
    invalid_text: (string),
    required_text: (string),
    optional_text: (string),
    label: (string),
    icons_path: (string),
    inner_check: (boolean)
#}

{% extends "@ecl-twig/ec-component-ecl-compliance/ecl-compliance.html.twig" %}
{% set component = 'datepicker' %}
{% set _inner_check = inner_check|default(false) %}
{% set not_compliant = icons_path is empty %}
{% set message = '{{ icons_path }} property needs to be set and filled in as it is needed to retrieve the icons sprite file.' %}

{% include '@ecl-twig/ec-component-ecl-compliance/components/ecl-compliance-form-group.html.twig' with {
  helper_id: helper_id,
  invalid: invalid,
  required: required,
  invalid_text: invalid_text,
  required_text: required_text,
  optional_text: optional_text,
  label: label,
  inner_check: true,
  icon_path: icons_path
} only %}
