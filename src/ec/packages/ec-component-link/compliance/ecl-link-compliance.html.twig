{% spaceless %}

{#
  Parameters:
  - "link" (object)
  - "icon" (object)
#}

{% set _message = [] %}

{% set compliance_check = link.label is empty and (icons[0].name is not defined or icons[0].name is empty) %}
{% set _message = _message|merge({ message : '- link.label or icon.name cannot be both empty, an unusable link tag would be generated otherwise' }) %}

{% if compliance_check %}{{- _message -}}{% endif %}

{% endspaceless %}
