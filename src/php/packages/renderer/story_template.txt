import { storiesOf } from '@storybook/html';
import #component_variant# from './#php_file_name#';

storiesOf('Components/#component#/#component_variant#', module)
  .add(
    'default',
    () => {
      return #component_variant#;
    }
  );
